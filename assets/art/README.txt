Hand-delivered illustrations. See ../../store/art-prompts.md for the brief and the
filenames the app expects. Missing files are safe: src/art.tsx renders nothing and the
screen keeps its text-only layout.
